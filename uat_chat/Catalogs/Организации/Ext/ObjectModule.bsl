
// Обработчик события "ПередЗаписью" Объекта
//
Процедура ПередЗаписью(Отказ)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;

	//Если НЕ ЭтоГруппа И НЕ ЗначениеЗаполнено(ЮрФизЛицо) тогда
	//	ОбщегоНазначения.СообщитьОбОшибке("Не указана принадлежность организации к юр./физ. лицу!", Отказ);
	//	Возврат;
	//КонецЕсли;
	
	Если ЮрФизЛицо = Перечисления.ЮрФизЛицо.ФизЛицо Тогда
		КПП       = "";
		КодПоОКПО = "";
	КонецЕсли; 
	
КонецПроцедуры
