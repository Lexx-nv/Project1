
Процедура ПередЗаписью(Отказ, Замещение)
	Для Каждого мЗаписьНабора Из ЭтотОбъект Цикл
		мЗаписьНабора.Лог = "" + ИмяКомпьютера() + " (" + ИмяПользователя() + ") " + ТекущаяДата();
	КонецЦикла;
КонецПроцедуры
